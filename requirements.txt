# Core dependencies
pathway==0.15.0
google-generativeai>=0.8.0  # Google Gemini API (replaces anthropic)
pandas>=2.0.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0

# Optional but recommended
tqdm>=4.65.0  # Progress bars for batch processing